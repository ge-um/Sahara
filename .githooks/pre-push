#!/bin/bash
echo "Running tests before push..."
make -s test 2>&1 | tail -5

if [ ${PIPESTATUS[0]} -ne 0 ]; then
    echo ""
    echo "Tests failed. Push aborted."
    echo "Fix the failing tests, or use 'git push --no-verify' to bypass."
    exit 1
fi

echo "All tests passed. Pushing..."
